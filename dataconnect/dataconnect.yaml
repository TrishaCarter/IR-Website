specVersion: "v1beta"
serviceId: "IR-Website"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "IR-Website-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
